avenger 1;
import { pass, shared } from 'shared.avenger';

chart cartesian as cartesian {
  data: {
    table: 'shared.points';
  }
  param 64.0 as point_size;
  transform pass {
  }
  mark symbol as points {
    size: encoded $point_size;
    x: encoded "x";
    y: encoded "y";
  }
}
