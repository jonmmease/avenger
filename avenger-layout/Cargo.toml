[package]
name = "avenger-layout"
version = "0.1.0"
edition = "2024"
description = "Chart-independent layout primitives for arranging rectangular regions whose content rectangles align while edge overflow is synchronized"

[dependencies]

[dev-dependencies]
resvg.workspace = true
