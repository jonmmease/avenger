# Frontend fixtures

These directories are populated phase by phase from the normative DSL spec.
`tokens/` stores lexical conformance cases; `parse/` and `resolution/` separate
valid and invalid sources; `definitions/` covers hygienic expansion. Fixtures
are reviewed inputs and must never be rewritten by a baseline update.
