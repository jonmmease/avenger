avenger 1;
chart cartesian as chart {
  param CAST(640.0 AS DOUBLE) as width;
  mark symbol as points {
    size: encoded $width / 64.0;
    x: encoded "Horsepower";
    y: encoded "Miles_per_Gallon";
  }
}
