# Chart version-1 fixture

`version-one.bin` encodes the original symbol-chart descriptor fields and an inline three-row source. It has artifact version 1 and omits the version-2 additions. The compatibility test reads this fixed wire fixture and checks the original defaults after decoding.
