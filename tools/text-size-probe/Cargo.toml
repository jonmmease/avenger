[package]
name = "text-size-probe"
version = "0.1.0"
edition = "2021"
publish = false

[workspace]

[features]
default = []
typst = ["dep:avenger-text"]

[dependencies]
avenger-text = { path = "../../avenger-text", default-features = false, optional = true }
